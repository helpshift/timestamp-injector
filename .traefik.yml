displayName: Timestamp Header
type: middleware
import: github.com/helpshift/timestamp-injector
summary: "Adds HS-UEpoch header with current time in seconds.milliseconds (like Nginx $msec)."
testData: {}
